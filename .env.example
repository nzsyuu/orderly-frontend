# Vazio = o Next faz proxy de /api para a API Java (evita CORS e envia o cookie JWT).
NEXT_PUBLIC_API_URL=

# Destino do proxy no servidor Next. Não precisa ser público.
API_PROXY_TARGET=http://localhost:8080
